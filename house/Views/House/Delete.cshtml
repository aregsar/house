@model int
@{
    ViewBag.Title = "Delete House";
}

<h3>House.Delete</h3>

<form method="post" asp-controller="House" asp-action="Destroy">
    <input type="hidden" name="id" value="@Model"/>
    <div class="form-group">
        <button type="submit" class="btn btn-primary btn-lg">Delete For Sure?</button>
    </div>
</form>
